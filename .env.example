# Rolligan web — Supabase Realtime (join-only). Copy to `.env.local` and fill in.
# Broadcast channels need only the anon (publishable) key — no DB tables/RLS.
# Dedicated Rolligan project (ref hofbysxskkxzhxhdtgbv). These are PUBLIC client
# keys (same ones the iOS app ships) — safe to commit. Set the same two values in
# the production host env (Vercel) and redeploy to move web off the YulePick project.
VITE_SUPABASE_URL=https://hofbysxskkxzhxhdtgbv.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_JgdT2HO1vFpnIxfur2G2Iw_MOH83OwF
